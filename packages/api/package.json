{
    "name": "@pappus/api",
    "dependencies": {
        "tsup": "^6.2.3"
    },
    "main": "./dist/index.mjs",
    "scripts": {
        "dev": "npx tsup --watch",
        "build": "npx tsup --minify"
    }
}